package com.example.quizhub.entity.enums;

public enum NotificationType {
    QUESTION_APPROVED,
    QUESTION_REJECTED,
    JOIN_REQUEST,
    JOIN_APPROVED,
    QUIZ_SUBMITTED,
    QUIZ_ASSIGNED,
    JOIN_REJECTED,
    SYSTEM_ALERT
}
